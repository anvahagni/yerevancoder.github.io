import React from 'react';

export default ({
  posted_at,
  day_diff,
  hour_diff,
  minute_diff,
  poster,
  post_key,
  title,
  up_votes,
}) => {
  // console.log(props);
  return (
    <div>
      <p>foo</p>
    </div>
  );
};
